-- Crear base de datos
CREATE DATABASE IF NOT EXISTS delux_hotel;
USE delux_hotel;
CREATE TABLE IF NOT EXISTS usuarios (
  id_usuario INT AUTO_INCREMENT PRIMARY KEY,
  nombre VARCHAR(100) NOT NULL,
  correo VARCHAR(100) NOT NULL UNIQUE,
  contrasena VARCHAR(255) NOT NULL,
  rol ENUM('cliente', 'admin') NOT NULL DEFAULT 'cliente'
);
CREATE TABLE IF NOT EXISTS habitaciones (
  id_habitacion INT AUTO_INCREMENT PRIMARY KEY,
  tipo_habitacion VARCHAR(50) NOT NULL, -- Doble, Suite, Penthouse, etc.
  descripcion TEXT,
  precio DECIMAL(10, 2) NOT NULL,
  capacidad INT NOT NULL,
  disponible BOOLEAN DEFAULT TRUE,
  ciudad VARCHAR(100) NOT NULL
);
CREATE TABLE IF NOT EXISTS reservas (
  id_reserva INT AUTO_INCREMENT PRIMARY KEY,
  id_usuario INT NOT NULL,
  id_habitacion INT NOT NULL,
  fecha_inicio DATE NOT NULL,
  fecha_fin DATE NOT NULL,
  estado ENUM('activa', 'cancelada', 'finalizada') DEFAULT 'activa',
  FOREIGN KEY (id_usuario) REFERENCES usuarios(id_usuario) ON DELETE CASCADE,
  FOREIGN KEY (id_habitacion) REFERENCES habitaciones(id_habitacion) ON DELETE CASCADE
);
INSERT INTO usuarios (nombre, correo, contrasena, rol) VALUES
('Admin', 'admin@delux.com', '123456', 'admin'),
('Daniel', 'daniel@delux.com', '123456', 'cliente'),
('Prueba MySQL', 'prueba@correo.com', 'hashprueba', 'cliente');
INSERT INTO habitaciones (tipo_habitacion, descripcion, precio, capacidad, ciudad) VALUES
('Doble', 'Cama doble para 2 personas, baño privado, wifi', 180.00, 2, 'Bogotá'),
('Familiar', 'Cama doble + sencilla, ideal para 3 personas', 240.00, 3, 'Medellín'),
('Suite', 'Suite con jacuzzi, minibar, ideal para 2 personas', 320.00, 2, 'Cartagena'),
('Penthouse', 'Penthouse premium con vista al mar y jacuzzi', 550.00, 4, 'Santa Marta');
-- Ver columnas de la tabla usuarios
SHOW COLUMNS FROM usuarios LIKE 'rol';

-- Asegurar que rol tenga valores por defecto correctos
ALTER TABLE usuarios MODIFY rol ENUM('cliente', 'admin') NOT NULL DEFAULT 'cliente';

-- Ver datos insertados
SELECT * FROM usuarios;
SELECT * FROM habitaciones;
